rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users to read/write during initial setup
    function isAuth() { return request.auth != null; }

    match /{document=**} {
      allow read, write: if isAuth();
    }
  }
}
